# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## данные

в приложении используются данные о товарах, данные покупателя, данные для отправки заказа на сервер и данные, которые приходят от сервера в ответ на запросы.

### тип `TPayment`

тип описывает доступные способы оплаты заказа. значение может быть только одним из двух вариантов: `card` или `cash`.

```ts
export type TPayment = 'card' | 'cash';
```

### интерфейс `IProduct`

интерфейс описывает товар, который отображается в каталоге, может быть открыт для подробного просмотра и добавлен в корзину.

```ts
export interface IProduct {
    id: string;
    description: string;
    image: string;
    title: string;
    category: string;
    price: number | null;
}
```

поля интерфейса:

- `id: string` — уникальный идентификатор товара;
- `description: string` — описание товара;
- `image: string` — путь к изображению товара;
- `title: string` — название товара;
- `category: string` — категория товара;
- `price: number | null` — цена товара. если значение равно `null`, товар недоступен для покупки.

### интерфейс `IBuyer`

интерфейс описывает данные покупателя, необходимые для оформления заказа.

```ts
export interface IBuyer {
    payment: TPayment;
    email: string;
    phone: string;
    address: string;
}
```

поля интерфейса:

- `payment: TPayment` — выбранный способ оплаты;
- `email: string` — электронная почта покупателя;
- `phone: string` — телефон покупателя;
- `address: string` — адрес доставки.

### интерфейс `IProductsResponse`

интерфейс описывает ответ сервера при получении каталога товаров.

```ts
export interface IProductsResponse {
    total: number;
    items: IProduct[];
}
```

поля интерфейса:

- `total: number` — общее количество товаров;
- `items: IProduct[]` — массив товаров.

### интерфейс `IOrderRequest`

интерфейс описывает объект, который отправляется на сервер при оформлении заказа.

```ts
export interface IOrderRequest extends IBuyer {
    total: number;
    items: string[];
}
```

поля интерфейса:

- `payment: TPayment` — выбранный способ оплаты;
- `email: string` — электронная почта покупателя;
- `phone: string` — телефон покупателя;
- `address: string` — адрес доставки;
- `total: number` — итоговая стоимость заказа;
- `items: string[]` — массив идентификаторов товаров в заказе.

### интерфейс `IOrderResponse`

интерфейс описывает ответ сервера после успешного оформления заказа.

```ts
export interface IOrderResponse {
    id: string;
    total: number;
}
```

поля интерфейса:

- `id: string` — идентификатор созданного заказа;
- `total: number` — итоговая стоимость заказа.

## модели данных

### класс `Products`

класс отвечает за хранение каталога товаров и товара, выбранного для подробного просмотра. модель не работает с DOM-элементами и не отвечает за отображение данных на странице.

конструктор класса не принимает параметров.

поля класса:

- `items: IProduct[]` — массив всех товаров каталога;
- `selectedProduct: IProduct | null` — товар, выбранный для подробного просмотра. если товар не выбран, хранится значение `null`.

методы класса:

#### `setItems(items: IProduct[]): void`

метод сохраняет массив товаров в модель.

параметры:

- `items: IProduct[]` — массив товаров, полученный из тестовых данных или с сервера.

возвращаемое значение: `void`.

#### `getItems(): IProduct[]`

метод возвращает массив всех товаров, сохранённых в модели.

возвращаемое значение:

- `IProduct[]` — массив товаров.

#### `getProduct(id: string): IProduct | undefined`

метод ищет товар в каталоге по его идентификатору.

параметры:

- `id: string` — идентификатор товара.

возвращаемое значение:

- `IProduct | undefined` — найденный товар или `undefined`, если товара с таким идентификатором нет.

#### `setSelectedProduct(product: IProduct): void`

метод сохраняет товар, выбранный для подробного просмотра.

параметры:

- `product: IProduct` — выбранный товар.

возвращаемое значение: `void`.

#### `getSelectedProduct(): IProduct | null`

метод возвращает товар, выбранный для подробного просмотра.

возвращаемое значение:

- `IProduct | null` — выбранный товар или `null`, если товар не выбран.

### класс `Basket`

класс отвечает за хранение товаров, которые покупатель добавил в корзину. модель не работает с DOM-элементами и не отвечает за отображение корзины на странице.

конструктор класса не принимает параметров.

поля класса:

- `items: IProduct[]` — массив товаров, добавленных в корзину.

методы класса:

#### `getItems(): IProduct[]`

метод возвращает массив товаров, которые находятся в корзине.

возвращаемое значение:

- `IProduct[]` — массив товаров в корзине.

#### `addItem(product: IProduct): void`

метод добавляет товар в корзину.

параметры:

- `product: IProduct` — товар, который нужно добавить в корзину.

возвращаемое значение: `void`.

#### `removeItem(product: IProduct): void`

метод удаляет товар из корзины по его идентификатору.

параметры:

- `product: IProduct` — товар, который нужно удалить из корзины.

возвращаемое значение: `void`.

#### `clear(): void`

метод полностью очищает корзину.

возвращаемое значение: `void`.

#### `getTotalPrice(): number`

метод возвращает общую стоимость товаров в корзине. если у товара цена равна `null`, при подсчёте она считается как `0`.

возвращаемое значение:

- `number` — итоговая стоимость товаров в корзине.

#### `getCount(): number`

метод возвращает количество товаров в корзине.

возвращаемое значение:

- `number` — количество товаров в корзине.

#### `hasItem(id: string): boolean`

метод проверяет, находится ли товар в корзине.

параметры:

- `id: string` — идентификатор товара.

возвращаемое значение:

- `boolean` — `true`, если товар есть в корзине, и `false`, если товара в корзине нет.

### класс `Buyer`

класс отвечает за хранение данных покупателя, необходимых для оформления заказа. модель позволяет сохранять данные частями, получать все сохранённые данные, очищать их и проверять заполненность полей.

конструктор класса не принимает параметров.

поля класса:

- `payment: IBuyer['payment'] | null` — выбранный способ оплаты. если способ оплаты не выбран, хранится значение `null`;
- `email: string` — электронная почта покупателя;
- `phone: string` — телефон покупателя;
- `address: string` — адрес доставки.

методы класса:

#### `setData(data: Partial<IBuyer>): void`

метод сохраняет данные покупателя в модель. можно передать только часть данных, например только адрес или только телефон. уже сохранённые значения при этом не удаляются.

параметры:

- `data: Partial<IBuyer>` — объект с одним или несколькими полями покупателя.

возвращаемое значение: `void`.

#### `getData(): IBuyer`

метод возвращает все данные покупателя.

возвращаемое значение:

- `IBuyer` — объект с данными покупателя.

#### `clear(): void`

метод очищает данные покупателя.

возвращаемое значение: `void`.

#### `validate(): TBuyerErrors`

метод проверяет заполненность полей покупателя и возвращает объект с ошибками. если поле заполнено корректно, ошибка для него не добавляется.

возвращаемое значение:

- `TBuyerErrors` — объект, в котором ключами являются поля покупателя, а значениями — тексты ошибок.

пример объекта ошибок:

```ts
{
    payment: 'не выбран способ оплаты',
    email: 'укажите email',
}
```

## слой коммуникации

### класс `WebLarekApi`

класс отвечает за взаимодействие приложения с сервером. он использует объект, соответствующий интерфейсу `IApi`, и через него выполняет запросы к api. класс не хранит данные приложения и не работает с DOM-элементами.

конструктор класса:

```ts
constructor(api: IApi)
```

параметры конструктора:

- `api: IApi` — объект api-клиента, который содержит методы `get` и `post`.

поля класса:

- `api: IApi` — экземпляр api-клиента, используемый для отправки запросов на сервер.

методы класса:

#### `getProducts(): Promise<IProductsResponse>`

метод выполняет `GET`-запрос на эндпоинт `/product/` и получает каталог товаров с сервера.

возвращаемое значение:

- `Promise<IProductsResponse>` — промис с объектом, в котором находится общее количество товаров и массив товаров.

#### `orderProducts(order: IOrderRequest): Promise<IOrderResponse>`

метод выполняет `POST`-запрос на эндпоинт `/order/` и отправляет данные заказа на сервер.

параметры:

- `order: IOrderRequest` — объект с данными покупателя, итоговой стоимостью заказа и массивом идентификаторов товаров.

возвращаемое значение:

- `Promise<IOrderResponse>` — промис с объектом, подтверждающим успешное оформление заказа.

## слой представления

слой представления отвечает за отображение интерфейса и генерацию событий в ответ на действия пользователя. классы представления не хранят данные приложения и не принимают самостоятельных решений об изменении состояния. все изменения данных выполняются через модели, а логика взаимодействия компонентов находится в презентере.

### page

класс отвечает за главную страницу приложения.

отвечает за:
- отображение каталога товаров;
- отображение счётчика товаров в корзине;
- обработку нажатия на кнопку открытия корзины.

основные элементы:
- контейнер каталога товаров;
- кнопка корзины;
- счётчик корзины.

события:
- `basket:open` — пользователь нажал на кнопку открытия корзины.

### modal

класс отвечает за модальное окно.

отвечает за:
- открытие модального окна;
- закрытие модального окна;
- установку содержимого модального окна;
- управление модификатором `modal_active`.

модальное окно не имеет дочерних классов. компоненты, которые отображаются внутри модального окна, являются самостоятельными компонентами представления.

### card

абстрактный родительский класс для карточек товара.

отвечает за общий функционал карточек:
- отображение названия товара;
- отображение цены товара;
- отображение изображения товара;
- отображение категории товара;
- применение модификаторов категории через `categoryMap`.

от этого класса наследуются карточки для разных шаблонов.

### catalogcard

класс отвечает за карточку товара в каталоге.

отвечает за:
- отображение краткой информации о товаре;
- обработку выбора карточки пользователем.

события:
- `card:select` — пользователь выбрал карточку товара для просмотра.

### previewcard

класс отвечает за карточку товара в модальном окне просмотра.

отвечает за:
- отображение полной информации о товаре;
- отображение описания товара;
- отображение кнопки покупки товара.

события:
- `product:buy` — пользователь нажал кнопку покупки товара.

### basketcard

класс отвечает за карточку товара в корзине.

отвечает за:
- отображение названия товара;
- отображение цены товара;
- отображение порядкового номера товара в корзине;
- обработку удаления товара из корзины.

события:
- `basket:item-delete` — пользователь нажал кнопку удаления товара из корзины.

### basketview

класс отвечает за отображение корзины.

отвечает за:
- отображение списка товаров в корзине;
- отображение итоговой стоимости;
- состояние кнопки оформления заказа;
- обработку перехода к оформлению заказа.

события:
- `order:open` — пользователь нажал кнопку оформления заказа.

### form

абстрактный родительский класс для форм.

отвечает за общий функционал форм:
- хранение ссылки на элемент формы;
- хранение ссылки на кнопку отправки формы;
- хранение ссылки на элемент отображения ошибок;
- отображение ошибок;
- управление состоянием кнопки отправки;
- обработку ввода данных;
- обработку отправки формы.

### orderform

класс отвечает за первую форму оформления заказа.

отвечает за:
- выбор способа оплаты;
- ввод адреса доставки;
- применение модификатора `button_alt-active` для выбранного способа оплаты;
- переход ко второй форме оформления заказа.

события:
- `order:change` — пользователь изменил данные первой формы заказа;
- `order:submit` — пользователь нажал кнопку перехода ко второй форме.

### contactsform

класс отвечает за вторую форму оформления заказа.

отвечает за:
- ввод email;
- ввод телефона;
- завершение оформления заказа.

события:
- `contacts:change` — пользователь изменил контактные данные;
- `contacts:submit` — пользователь нажал кнопку оплаты и завершения оформления заказа.

### success

класс отвечает за отображение окна успешного оформления заказа.

отвечает за:
- отображение суммы заказа;
- обработку закрытия окна успешного заказа.

события:
- `success:close` — пользователь нажал кнопку закрытия окна успешного заказа.

## события приложения

### события моделей данных

- `products:changed` — изменён список товаров каталога.
- `product:selected` — изменён выбранный для просмотра товар.
- `basket:changed` — изменилось содержимое корзины.
- `buyer:changed` — изменились данные покупателя.

### события представления

- `card:select` — пользователь выбрал карточку товара в каталоге.
- `product:buy` — пользователь нажал кнопку покупки товара.
- `basket:item-delete` — пользователь нажал кнопку удаления товара из корзины.
- `basket:open` — пользователь нажал кнопку открытия корзины.
- `order:open` — пользователь нажал кнопку оформления заказа.
- `order:change` — пользователь изменил данные первой формы оформления заказа.
- `order:submit` — пользователь нажал кнопку перехода ко второй форме оформления заказа.
- `contacts:change` — пользователь изменил данные второй формы оформления заказа.
- `contacts:submit` — пользователь нажал кнопку оплаты и завершения оформления заказа.
- `success:close` — пользователь нажал кнопку закрытия окна успешного заказа.

## презентер

в приложении используется один презентер, так как приложение состоит из одной страницы. презентер реализован в файле `src/main.ts`.

презентер отвечает за связь между слоем данных и слоем представления. он подписывается на события, которые генерируют модели данных и компоненты представления, и выполняет необходимые действия: изменяет данные в моделях, открывает модальные окна, передаёт данные в компоненты представления и отправляет заказ на сервер.

презентер не хранит данные приложения. за хранение каталога товаров отвечает модель `products`, за хранение корзины — модель `basket`, за хранение данных покупателя — модель `buyer`.

презентер также не генерирует события самостоятельно как результат изменения данных. он обрабатывает события пользовательских действий и вызывает методы моделей. после изменения данных модели генерируют собственные события, на которые презентер реагирует перерисовкой соответствующих компонентов.

### основные сценарии работы презентера

1. после загрузки страницы презентер запрашивает каталог товаров через `weblarekapi` и сохраняет полученные товары в модель `products`.
2. после события `products:changed` презентер получает товары из модели и отрисовывает каталог на главной странице.
3. при событии `card:select` презентер сохраняет выбранный товар в модель `products`.
4. после события `product:selected` презентер открывает модальное окно с подробной карточкой товара.
5. при событии `product:buy` презентер добавляет товар в корзину через модель `basket`.
6. после события `basket:changed` презентер обновляет счётчик корзины и состояние каталога.
7. при событии `basket:open` презентер открывает модальное окно корзины.
8. при событии `basket:item-delete` презентер удаляет товар из корзины через модель `basket`.
9. при событии `order:open` презентер открывает первую форму оформления заказа.
10. при событиях `order:change` и `contacts:change` презентер сохраняет данные покупателя в модель `buyer`.
11. после события `buyer:changed` презентер обновляет открытую форму оформления заказа.
12. при событии `order:submit` презентер проверяет данные первой формы и открывает форму контактов.
13. при событии `contacts:submit` презентер проверяет данные второй формы, собирает заказ и отправляет его на сервер.
14. после успешного оформления заказа презентер очищает корзину и данные покупателя, затем показывает окно успешного заказа.
15. при событии `success:close` презентер закрывает модальное окно.